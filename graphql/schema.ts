// const { makeExecutableSchema } = require('graphql-tools');
// const { buildTypeDefsAndResolvers } = require('type-graphql');
// const ArticleResolver = require('./resolvers/articlesResolvers');

// //TODO See if this no will be a potencial perform problem
// const { typeDefs, resolvers } = (async () => await buildTypeDefsAndResolvers({
//   resolvers: [ArticleResolver],
//   validate: false,
// }))();

// module.exports = makeExecutableSchema({ typeDefs, resolvers });
